<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}MissionHub{% endblock %}</title>
    {% load static %}
    <link rel="stylesheet" href="{% static 'css/style.css' %}">
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f4; }
        nav { background: #333; color: white; padding: 1rem; display: flex; justify-content: space-between; align-items: center; }
        nav a { color: white; text-decoration: none; margin: 0 10px; }
        nav .user-info { display: flex; align-items: center; }
        .container { padding: 20px; max-width: 1200px; margin: auto; background: white; min-height: 80vh;}
        .messages { list-style: none; padding: 0; margin: 0 0 20px 0; }
        .messages li { padding: 15px; margin-bottom: 15px; border-radius: 4px; }
        .messages li.success { background-color: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
        .messages li.error { background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
        .messages li.info { background-color: #d1ecf1; color: #0c5460; border: 1px solid #bee5eb; }
    </style>
    <script src="https://sdk.pi-network.com/v2/pi-sdk.js"></script>
    <script>Pi.init({ version: "2.0", sandbox: true })</script>
</head>
<body>
    <nav>
        <div class="nav-links">
            <a href="{% url 'list_missions' %}"><strong>MissionHub</strong></a>
            {% if user.is_authenticated %}
                <a href="{% url 'my_proofs' %}">Mes Preuves</a>
                <a href="{% url 'user_notifications' %}">Notifications</a>
            {% endif %}
        </div>
        <div class="user-info">
            {% if user.is_authenticated %}
                <span>Bonjour, <a href="{% url 'user_profile' %}">{{ user.profile.pseudo }}</a></span>
                <span style="margin-left: 20px;">Solde : <strong>{{ user.profile.solde|default:"0.0" }} π</strong></span>
                <a href="{% url 'logout' %}" style="margin-left: 20px;">Déconnexion</a>
            {% else %}
                <a href="{% url 'login' %}">Connexion</a>
                <a href="{% url 'signup' %}">Inscription</a>
            {% endif %}
        </div>
    </nav>

    <main class="container">
        {% if messages %}
            <ul class="messages">
                {% for message in messages %}
                    <li class="{{ message.tags }}">{{ message }}</li>
                {% endfor %}
            </ul>
        {% endif %}

        {% block content %}{% endblock %}
    </main>

</body>
</html>

